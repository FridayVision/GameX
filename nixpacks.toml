[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.install]
cmds = [
  "npm ci",
  "pip3 install -r tools/requirements.txt --break-system-packages"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
